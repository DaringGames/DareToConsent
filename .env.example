# DareToConsent deployment configuration (example)
# Copy to .env and adjust values for your local environment. Safe to commit; contains no secrets.

# AWS environment
AWS_PROFILE=YOUR_AWS_PROFILE
AWS_REGION=YOUR_AWS_REGION

# Remote target used by deploy script
DEFAULT_REMOTE=ec2-user@YOUR_EC2_HOST_OR_IP
DEFAULT_DOMAIN=your-domain.example.com

# SSH
EC2_SSH_KEY_PATH=~/.ssh/YOUR_KEY.pem
# Optional override for SSH:
# SSH_OPTS=-i ~/.ssh/YOUR_KEY.pem -o StrictHostKeyChecking=no

# App location/process
APP_DIR=/opt/daretoconsent
APP_HOST=127.0.0.1
APP_PORT=3001
SERVICE_NAME=daretoconsent

# TLS/ACME
CERTBOT_EMAIL=you@example.com

# Shared infrastructure (non-secret identifiers)
ASSETS_BUCKET=your-s3-bucket
SSM_NAMESPACE=/your-stack/daretoconsent

# Notes:
# - [deploy.sh](deploy/deploy.sh:1) will source .env on your local machine for these values.
# - Do NOT commit a real .env file; it is ignored by git.
# - Secrets (e.g., DATABASE_URL, JWT keys) should be stored in AWS SSM under $SSM_NAMESPACE.
# - Server runtime env (PORT/HOST) is set by PM2 via the generated ecosystem config; no .env is
#   required on the server for this app unless you intentionally add one in /opt/.../shared/.